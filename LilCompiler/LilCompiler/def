program
{
    int a;
    bool b;
    double c;

    a = 5;
    a = -~-a;
    b = !!!b;

    write a * a;
    { return; }
    write a * a + a / a - a == a || a > a;
    return;
    write c;
}

 