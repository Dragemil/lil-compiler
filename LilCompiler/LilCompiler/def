program
{
    int i;
    bool b;

    b = !!!b;

    if (b)
    {
        if (!b)
            { write "no ok"; }
        else
            write "Hura!\n";
    }

    while (i < 5)
        write i = i + 1;
    write "\n";

    //write b && b || b;
    return;
}

