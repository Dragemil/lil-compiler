program
{
    int a;
    bool b;
    double c;

    a = 5;
    a = -~-a;
    b = !!b; //let's go

    write b && b || b;
    { return; }
    write a * a + a / a - a == a || a > a;
    return;
    write c;
}

